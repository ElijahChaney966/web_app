export default () => {
        var name = users.map(user => users.name)

    var users = [
        {name: "Elijah",
        name: "Tim",
        name: "Stefanie"}
    ];

    var usernames = [
        {usernames: "elijah2017",
            usernames: "stefanie1982",
            usernames: "tim1958"
        }
    ];

    for (const user of usernames) {
        usernames.push(user.name)
    }
    for (var user_name of users){
        users.push(users.name)
    }
}

